<div class="row">
  <div class="col s12 m6 offset-m3 center">
    <blockquote>The books that help you most are those which make you think that most.
       The hardest way of learning is that of easy reading;
       but a great book that comes from a great thinker is a ship of thought,
       deep freighted with truth and beauty.
    </blockquote>
    <p>–Pablo Neruda</p>
    click to see description
      <ul class="collapsible" data-collapsible="accordion">
        <% @books.each do |book| %>
        <li>
          <div class="collapsible-header">
            <strong><%= book.name.capitalize %>
              <small><%= link_to "Reviews", reviews_path(book.id), class:"waves-effect waves-light" %></small></strong>

            Author: <%= book.author %>
          </div>
          <div class="collapsible-body">
            <p><%= book.description %></p>
          </div>
        </li>
        <% end %>
      </ul>
  </div>
</div>
