<div class="row">
  <div class="col s12 m6 offset-m3 center">
    <blockquote>A lot of things have been thrown at me in life,
      and I've got through it all without a rule book, taking it one day at a time.
    </blockquote>
    <p>–Yoko Ono</p>
    <%= form_for Book.new, url: book_create_url, :method => 'post' do |f| %>
      <div class="input-field"><i class="material-icons prefix">mode_edit</i>
        <%= f.text_field :name, autofocus: true, :class => "validate", :placeholder => "Name" %>
      </div>
      <div class="input-field"><i class="material-icons prefix">mode_edit</i>
        <%= f.text_field :author, autofocus: true, :class => "validate", :placeholder => "Author" %>
      </div>
      <div class="input-field"><i class="material-icons prefix">mode_edit</i>
        <%= f.text_area :description, autofocus: true, :class => "validate", :placeholder => "Description" %>
      </div>
      <button class="btn waves-effect waves-light" type="submit" name="action">Save</button>
    <% end %>
  </div>
</div>
