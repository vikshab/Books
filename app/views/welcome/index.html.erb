<div class="container">
  <div class="row">
    <div class="col s12 m6 offset-m3 center">
      <div class="container1">
      <div class="circle">
        <p><%= image_tag("logo.gif") %></p>
      </div>
    </div>

      <br><br><blockquote>Keep reading books, but remember that a book is only a book,
        and you should learn to think for yourself.
      </blockquote>
      <p>–Maxim Gorky</p>

      <div class="search">
        <%= form_for :search, url: search_path do |f| %>
          <div class="input-field">
            <%= f.text_field :query, placeholder: "Book name" %>
          </div>
          <button class="btn waves-effect waves-light" type="submit" name="action">Search</button>
        <% end %>
      </div>
    </div>
  </div>
</div>
